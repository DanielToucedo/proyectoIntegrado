
<?php
$servername = "localhost";
$username = "id20916213_localhost";
$password = "Adminadmin1-";
$basedatos = "id20916213_catastro";


try {
$familia=$_POST["familia"];

  $conn = new PDO("mysql:host=$servername;dbname=$basedatos", $username, $password);
  $conn->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
  $stmt = $conn->prepare("SELECT persona.dni, persona.nombre_persona, persona.apellidos_persona from persona WHERE persona.dni IN (SELECT persona_familia.dniPersona FROM persona_familia WHERE persona_familia.id_familia = \"$familia\")");

  $stmt->execute();

  echo JSON_ENCODE($stmt->fetchAll());

} catch(PDOException $e) {
  echo "Error: " . $e->getMessage();
}

	


$conn = null;
?>
